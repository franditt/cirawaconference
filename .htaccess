<IfModule mod_rewrite.c>
    RewriteEngine On

    # Prevent redirect loops when /public is already present
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>

# Prefer the public front controller
DirectoryIndex public/index.php
